server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API proxy — strip /api prefix, forward to FastAPI
    # resolver forces nginx to re-resolve 'server' on each request instead of
    # caching the IP at startup — prevents 502s after container restarts.
    resolver 127.0.0.11 valid=10s;
    set $upstream http://server:8000;

    location /api/ {
        rewrite            ^/api/(.*)$ /$1 break;
        proxy_pass         $upstream;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;

        # SSE / chunked streaming — disable buffering
        proxy_buffering        off;
        proxy_cache            off;
        proxy_read_timeout     3600s;
        proxy_send_timeout     3600s;

        # WebSocket upgrade
        proxy_set_header   Upgrade    $http_upgrade;
        proxy_set_header   Connection $connection_upgrade;
    }

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/javascript;
    gzip_min_length 1024;
}

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
